# Task plan (pointer)

Canonical plan + execution log: `pkgs/unreal/plan.md`.

---

## Context restart checklist (must do)

When resuming this task after any context reset:
1) Read `pkgs/unreal/plan.md` (single source of truth).
2) Read `pkgs/unreal/findings.md` (discovered root causes + hypotheses).
3) Read `pkgs/unreal/progress.md` (what we already tried + outcomes).

